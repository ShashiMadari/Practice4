- name: Deploying the War file to Tomcat Server
  hosts: web
  become: yes
  
  tasks:
  - name: Copying war file
    copy:
      src: "target/MyAnsibleApp.war"
      dest: "/opt/tomcat/webapps/"
      remote_src: no
    
  - name: Restart tomcat
    systemd:
      name: tomcat
      state: restarted
       
